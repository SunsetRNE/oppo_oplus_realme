# 🔄 同步变更对照（Sync Log）

> 用途：记录每次上游同步带来的**变更内容**，供后续优化/转化决策参考
> 更新时机：每次执行同步后（方法见 `docs/TRACKING.md`）
> 结构：按时间倒序，每条记录 = 日期 + 仓库 + 变更摘要 + 影响评估 + 处理动作

---

## 2026-08-03 ｜ 三主仓首次批量同步

### 1. oppo_oplus_realme_sm8850（bea18f0 → 69ab979，+6 commits）
| 变更 | 说明 | 影响评估 |
|---|---|---|
| 新增 6.12.58 构建脚本 | 上游新增 `local/builder_6.12.58.sh` 及对应 workflow | 🟢 新增能力，不影响既有 6.12.23 链路 |
| 新增 CVE 补丁 | 内核安全补丁目录更新 | 🟢 补丁随构建拉取，无破坏性 |

**处理**：`git pull --ff-only` 快进合并 ✅；未改动本地定制；新版本待验证后按发布流程测试。

### 2. oppo_oplus_realme_sm8750（b4f1ce4 → 21db9e7，+4 commits）
| 变更 | 说明 | 影响评估 |
|---|---|---|
| 新增 6.6.118 构建脚本 | 上游新增 `local/builder_6.6.118.sh` 及对应 workflow | 🟢 新增能力，不影响既有 6.6.89 链路 |
| 新增 CVE 补丁 | 内核安全补丁目录更新 | 🟢 无破坏性 |

**处理**：快进合并 ✅；未改动本地定制。

### 3. oppo_oplus_realme_sm8650（c9f0da1，无变化）
- 仅新增 tags，无代码变更。

---

## 📌 同步观察（供后续决策）

- **上游迭代模式**：以"新增内核小版本 + CVE 补丁"为主，**极少修改既有脚本逻辑** → 快进合并策略风险低
- **未同步项**：上游如有删除/重命名脚本的提交，需人工确认本地引用；当前未遇到
- **转化候选**：上游新增版本脚本与既有脚本差异 = 版本号 + 补丁路径，**可用模板化生成**（后续优化方向）

---

## 🔗 相关文档

- 同步状态表：`docs/TRACKING.md`
- 发布流程：`docs/RELEASE_PROCESS.md`
- 决策记录：`docs/DECISIONS.md`
